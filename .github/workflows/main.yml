name: PEP8 Validation and Autofix

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install PEP8 linter
        run: pip install pep8

      - name: Install autopep8
        run: pip install autopep8

      - name: Run PEP8 linter
        run: pep8 .

      - name: Fix PEP8 violations
        run: autopep8 -i .

      - name: Check code again for PEP8 violations
        run: pep8 .

      - name: Check PEP8 report for errors
        if: ${{ steps.pep8.outputs.failed }}
        run: exit 1
        
